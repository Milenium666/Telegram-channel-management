# Telegram Channel Management

Веб-приложение для управления Telegram-каналами с возможностью добавления, просмотра и удаления каналов через удобный интерфейс.

## 🚀 Основные возможности

- **Добавление каналов** - создание новых каналов через QR-модальное окно
- **Управление каналами** - просмотр списка каналов в табличном виде
- **Удаление каналов** - удаление каналов с подтверждением
- **Сохранение данных** - автоматическое сохранение состояния в localStorage
- **Восстановление данных** - автоматическое восстановление данных при перезагрузке страницы
- **Адаптивный дизайн** - интерфейс работает на различных устройствах

## 📋 Технологии

- **Vanilla JavaScript** (ES6+ модули)
- **HTML5**
- **CSS3** (BEM методология)
- **Vite** - современный сборщик для разработки

## 🛠 Установка

### Требования

- Node.js (версия 14 или выше)
- npm или yarn

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <https://github.com/Milenium666/Telegram-channel-management.git>
cd Telegram-channel-management
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev-сервер:
```bash
npm run dev
```

4. Откройте браузер и перейдите по адресу, который покажет Vite (обычно `http://localhost:5173`)

## 📜 Доступные команды

```bash
# Запуск dev-сервера с hot-reload
npm run dev

# Сборка проекта для production
npm run build

# Предпросмотр production сборки
npm run preview
```

## 📁 Структура проекта

```
Telegram-channel-management/
├── index.html              # Главная HTML-страница
├── package.json            # Конфигурация проекта и зависимости
├── src/
│   ├── main.js            # Основной JavaScript файл с логикой приложения
│   ├── style.css          # Главный файл стилей
│   ├── blocks/            # Блоки стилей (BEM методология)
│   │   ├── action-modal/  # Стили модального окна действий
│   │   ├── qr-modal/      # Стили QR-модального окна
│   │   ├── root/          # Корневые стили
│   │   ├── search-form/   # Стили формы поиска
│   │   └── table/         # Стили таблицы каналов
│   ├── images/            # Изображения и иконки
│   │   ├── icon-tg.svg
│   │   ├── menu-dots-vertical.svg
│   │   └── qr.png
│   └── vendor/            # Сторонние библиотеки
│       └── normalize.css  # CSS нормализация
└── public/                # Статические файлы
```

## 💡 Использование

### Добавление канала

1. Нажмите на кнопку "+ Добавить канал" или введите текст в поле поиска и нажмите Enter
2. В открывшемся модальном окне отобразится QR-код для авторизации
3. Отсканируйте QR-код через приложение Telegram (связанные устройства → добавить новое устройство)
4. Нажмите кнопку "Добавить канал" для завершения

### Управление каналом

1. Нажмите на кнопку с тремя точками (⋮) в строке нужного канала
2. В появившемся меню выберите:
   - **Настройки** - открыть настройки канала (в разработке)
   - **Удалить сессию** - удалить канал с подтверждением

### Данные канала

Каждый канал отображает следующую информацию:
- **Название** - номер канала
- **Воронка и этап** - категория и статус обработки
- **Данные аккаунта** - ID профиля
- **Статус** - текущий статус авторизации

## 💾 Хранение данных

Приложение использует **localStorage** браузера для сохранения данных:
- Список каналов сохраняется автоматически при добавлении или удалении
- Данные восстанавливаются при перезагрузке страницы
- При первом запуске данные извлекаются из HTML-разметки и сохраняются

**Ключи localStorage:**
- `telegram_channels` - массив каналов
- `telegram_channel_counter` - счетчик для генерации уникальных ID

## 🎨 Особенности интерфейса

- Модальные окна с затемнением фона
- Адаптивная таблица с поддержкой мобильных устройств
- Управление модальными окнами клавиатурой (ESC для закрытия)
- Позиционирование меню действий относительно кнопки
- Плавные анимации и переходы

## 🔧 Разработка

### Архитектура кода

Приложение построено на чистом JavaScript с использованием модульной структуры:
- Все обработчики событий привязаны в `DOMContentLoaded`
- Функции разделены по функциональности (сохранение, загрузка, работа с UI)
- Используется событийная модель для взаимодействия компонентов

### Ключевые функции

- `saveChannels()` - сохранение каналов в localStorage
- `loadChannels()` - загрузка каналов из localStorage
- `extractChannelsFromHTML()` - извлечение данных из HTML при первом запуске
- `createTableRow()` - создание строки таблицы
- `addChannel()` - добавление нового канала
- `deleteChannel()` - удаление канала
- `openQRModal()` / `closeQRModal()` - управление QR-модальным окном
- `openActionModal()` / `closeActionModal()` - управление модальным окном действий



## 📞 Поддержка

При возникновении проблем или вопросов создайте issue в репозитории проекта.
